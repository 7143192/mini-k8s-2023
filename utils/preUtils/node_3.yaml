apiVersion: v1
kind: Node
metadata:
  # character "/" is not allowed in the metadata.Name !!!
  # but character "_", "-" should be allowed.
  name: node3
  label: node3
  port: 9001
  ip: 192.168.1.10
  selector:
    gpu: nvidia